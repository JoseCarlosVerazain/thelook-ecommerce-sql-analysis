# TheLook E-commerce — SQL Analysis

Exploratory data analysis and business metrics on the TheLook
e-commerce public dataset (Google BigQuery).

## Approach
Each table is explored following a fixed protocol:
metadata → preview → volume → primary key integrity → data quality.

## Structure
- `queries/` — SQL scripts, numbered by analysis stage.
